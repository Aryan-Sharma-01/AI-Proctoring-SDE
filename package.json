{
  "name": "video-proctoring-system",
  "version": "1.0.0",
  "description": "A comprehensive video proctoring system for online interviews with focus detection and object recognition",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["video-proctoring", "interview", "focus-detection", "object-detection", "computer-vision"],
  "author": "Aryan Sharma",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
